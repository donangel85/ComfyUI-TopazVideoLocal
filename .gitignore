__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Local, machine-specific configuration — never commit
config.json
user_presets.json

# Logs and temporary run data
*.log
temp/

# Scratch space for working on this: experiments, notes, and the footage used to
# check the nodes against real material. None of that belongs in the package, and
# video files in particular are impossible to get back out of a git history.
research/
docs/
Testmaterial/
testmaterial/

# CI scratch: the captured pytest output, see .github/workflows/tests.yml
pytest.out
